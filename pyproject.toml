[project]
name = "uuid-tool"
version = "0.1.1"
description = "UUID generator and validator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
uuid-tool = "uuid_tool:main"

[dependency-groups]
dev = [
    "pytest~=9.0",
    "ruff~=0.15",
]
