[project]
name = "uuid-tool"
version = "0.2.0"
description = "UUID generator and validator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
uuid-tool = "uuid_tool:main"

[dependency-groups]
dev = [
    "pytest~=9.0",
    "ruff~=0.15",
]
